apiVersion: v1
kind: ServiceAccount
metadata:
  name: fitness-app-sa
  namespace: default
  annotations:
    # This Client ID tells Workload Identity which Azure Identity to assume
    # Replace the value below with the Client ID(managed identity)
    azure.workload.identity/client-id: "4ce26b3c-39bf-4b30-a0ac-fc40fb4d4c65"
  labels:
    azure.workload.identity/use: "true"
